import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    container:{
        alignItems: 'center',
        justifyContent: 'center',
        height: 250,
    },
     titulo:{
         fontSize: 24,
         fontWeight: 'bold',
         textAlign: 'center'
     }
});
